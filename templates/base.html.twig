<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>

        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>

    <body class="flex flex-col min-h-screen bg-gray-50">

        <!-- NAVIGATION -->
        <nav class="bg-[#1a237e] text-white py-4 shadow-md">
            <div class="container mx-auto flex items-center justify-between px-4">
                <span class="text-2xl font-bold tracking-tight">
                    Elias's <span class="text-blue-200">Portfolio</span>
                </span>
                <div class="flex space-x-8">
                    <a href="{{ path('home') }}" class="hover:text-blue-200 font-semibold transition">Home</a>
                    <a href="{{ path('about') }}" class="hover:text-blue-200 font-semibold transition">About</a>
                    <a href="{{ path('projects') }}" class="hover:text-blue-200 font-semibold transition">Projects</a>
                    <a href="{{ path('app_blog_post_index') }}" class="hover:text-blue-200 font-semibold transition">Blog</a>
                    <a href="{{ path('contact') }}" class="hover:text-blue-200 font-semibold transition">Contact Me</a>
                </div>
            </div>
        </nav>

        <!-- PAGE CONTENT -->
        <main class="flex-grow">
            <div class="container mx-auto px-4 py-12 max-w-6xl">
                {% block body %}{% endblock %}
            </div>
        </main>

        <!-- FOOTER -->
        <footer class="bg-slate-900 text-gray-200 text-center py-4 mt-8">
            &copy; {{ "now"|date("Y") }} Elias U. | Built with <a href="https://symfony.com" class="text-blue-300 hover:underline" target="_blank">Symfony</a> &amp; <a href="https://tailwindcss.com" class="text-blue-300 hover:underline" target="_blank">Tailwind CSS</a>
        </footer>

    </body>
</html>
